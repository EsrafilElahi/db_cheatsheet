sudo systemctl start postgresql

sudo systemctl stop postgresql

sudo systemctl restart postgresql

sudo -u postgres psql
CREATE USER your_username WITH SUPERUSER PASSWORD 'your_password';


\q --> exit
\l --> list pf db and user
\q --> connect to db
\du --> see users with permissions


sudo -u esrafil psql -d dbname


CREATE DATABASE database_name;
DROP DATABASE database_name;

ALTER USER your_username WITH SUPERUSER;

GRANT privilege(s) ON table_name TO username;

GRANT USAGE ON SCHEMA public TO your_user; // if permission denied , this work

ALTER USER username WITH PASSWORD 'new_password';
DROP USER username;

// backup db
pg_dump -U username -d database_name -f backup_file.sql

// restore db
psql -U username -d database_name -f backup_file.sql

